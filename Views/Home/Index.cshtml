@{
    Layout = "_Layout";
    ViewData["Title"] = "Home Page";
}

<style>
    #hero {
        position: relative;
        background: url('/static/hero.jpg') no-repeat center center / cover;
    }

    #hero:before {
        content: "";
        width: 100%;
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background-color: rgba(0, 0, 0, .85);
        z-index: 1;
    }

    #hero * {
        position: relative;
        z-index: 2;
    }
</style>

<div id="hero" class="text-center text-white py-20 min-h-screen flex flex-col justify-center items-center">
    <div class="px-5 max-w-4xl">
        <p class="text-2xl font-medium tracking-wide uppercase">Bring</p>
        <h1 class="typewrite text-5xl font-extrabold tracking-tighter leading-tight" data-period="2000" data-type='["Creativity", "Affordability", "Elegance"]'>
            <span class="wrap"></span>
        </h1>
        <p class="text-2xl font-medium uppercase mt-4">to your solution</p>
        <p class="text-lg mt-2 mb-6 leading-relaxed max-w-lg mx-auto">Learn how we can work for you</p>
        <a href="/contact" class="inline-block bg-transparent text-white py-3 px-8 rounded-lg border border-white hover:bg-white hover:text-black transition-colors duration-300 font-semibold text-lg">
            Talk to an Expert...
        </a>
    </div>
</div>

<div class="flex flex-wrap justify-center" style="background-color: #2F4F4F;">
    <div class="w-full text-center">
        <h2 class="text-5xl font-bold mt-10 mb-8 text-gray-200">Explore our other solutions</h2>
    </div>

    <div class="w-full md:w-1/3 px-4 mb-6">
        <div class="bg-gray-800 shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
            <div class="relative">
                <img src="~/static/shpe.png" class="w-full" alt="Project 1">
                <div class="absolute inset-0 bg-black opacity-60"></div>
                <div class="absolute inset-0 flex flex-col justify-center items-center p-6">
                    <h5 class="text-2xl font-semibold mb-3 text-white">SHPE MDC Club</h5>
                    <p class="text-gray-100 text-lg mb-5 text-center">Empowering the Hispanic community in STEM, SHPE MDC Club offers professional development, networking, and community service opportunities.</p>
                    <a href="https://www.shpemdc.com/" class="inline-block bg-blue-500 text-white py-3 px-6 rounded-lg hover:bg-blue-700">View Project</a>
                </div>
            </div>
        </div>
    </div>

    <div class="w-full md:w-1/3 px-4 mb-6">
        <div class="bg-gray-800 shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
            <div class="relative">
                <img src="~/static/workshop.png" class="w-full" alt="Project 2">
                <div class="absolute inset-0 bg-black opacity-60"></div>
                <div class="absolute inset-0 flex flex-col justify-center items-center p-6">
                    <h5 class="text-2xl font-semibold mb-3 text-white">Workshop Attendance App</h5>
                    <p class="text-gray-100 text-lg mb-5 text-center">Simplify event management with our Workshop Attendance App, featuring real-time tracking, easy check-ins, and comprehensive reporting.</p>
                    <a href="https://www.enteclr.com/" class="inline-block bg-blue-500 text-white py-3 px-6 rounded-lg hover:bg-blue-700">View Project</a>
                </div>
            </div>
        </div>
    </div>

    <div class="w-full md:w-1/3 px-4 mb-6">
        <div class="bg-gray-800 shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
            <div class="relative">
                <img src="~/static/wit.png" class="w-full" alt="Project 3">
                <div class="absolute inset-0 bg-black opacity-60"></div>
                <div class="absolute inset-0 flex flex-col justify-center items-center p-6">
                    <h5 class="text-2xl font-semibold mb-3 text-white">WiT MDC Club</h5>
                    <p class="text-gray-100 text-lg mb-5 text-center">Advancing women in technology through education, mentorship, and community projects. Join WiT MDC Club to make an impact.</p>
                    <a href="https://witmdc.com/" class="inline-block bg-blue-500 text-white py-3 px-6 rounded-lg hover:bg-blue-700">View Project</a>
                </div>
            </div>
        </div>
    </div>
</div>


<div id="about_us" style="background-color: #2F4F4F;" class="text-gray-100 py-12 px-4">
    <div class="max-w-6xl mx-auto">
        <h2 class="text-center text-4xl font-bold mb-10">About Us</h2>
        <div class="flex flex-wrap justify-center items-center gap-8">
            <div class="w-full md:w-1/2 px-4 text-center md:text-left">
                <h3 class="text-3xl font-semibold mb-6">Our Mission</h3>
                <p class="text-lg mb-6">
                    At <strong>PAH Solutions</strong>, we believe that high-quality software solutions should be accessible to everyone. 
                    Our mission is to make software solutions affordable and accessible, breaking down barriers that prevent small businesses, entrepreneurs, 
                    and startups from leveraging technology to grow and thrive.
                </p>
                <p class="text-lg">
                    Through innovation, dedication, and a deep understanding of our clients' needs, we aim to deliver exceptional value without the high cost typically associated with custom software. 
                    Our commitment is to provide cutting-edge solutions that empower our clients to achieve more with less.
                </p>
            </div>
        </div>
    </div>
</div>


<script>
    var TxtType = function(el, toRotate, period) {
        this.toRotate = toRotate;
        this.el = el;
        this.loopNum = 0;
        this.period = parseInt(period, 10) || 2000;
        this.txt = '';
        this.tick();
        this.isDeleting = false;
    };
    TxtType.prototype.tick = function() {
        var i = this.loopNum % this.toRotate.length;
        var fullTxt = this.toRotate[i];
        if (this.isDeleting) {
        this.txt = fullTxt.substring(0, this.txt.length - 1);
        } else {
        this.txt = fullTxt.substring(0, this.txt.length + 1);
        }
        this.el.innerHTML = '<span class="wrap">'+this.txt+'</span>';
        var that = this;
        var delta = 200 - Math.random() * 100;
        if (this.isDeleting) { delta /= 2; }
        if (!this.isDeleting && this.txt === fullTxt) {
        delta = this.period;
        this.isDeleting = true;
        } else if (this.isDeleting && this.txt === '') {
        this.isDeleting = false;
        this.loopNum++;
        delta = 500;
        }
        setTimeout(function() {
        that.tick();
        }, delta);
    };
    window.onload = function() {
        var elements = document.getElementsByClassName('typewrite');
        for (var i=0; i<elements.length; i++) {
            var toRotate = elements[i].getAttribute('data-type');
            var period = elements[i].getAttribute('data-period');
            if (toRotate) {
              new TxtType(elements[i], JSON.parse(toRotate), period);
            }
        }
        // INJECT CSS
        var css = document.createElement("style");
        css.type = "text/css";
        css.innerHTML = ".typewrite > .wrap { border-right: 0.08em solid #fff}";
        document.body.appendChild(css);
    };
</script>